import Colors from './Colors';

export const HeaderNavStyles = {
  NavBar: {
    headerStyle: { backgroundColor: Colors.primary, height: 50 },
    headerTintColor: Colors.primaryConstraint,
  },
};

export const TextInputStyles = {
  inputContainer: {
    borderWidth: 1,
    borderColor: Colors.grey,
    borderRadius: 3,
    borderBottomWidth: 1,
  },
};
export const SelectModalStyles = {
  rowContainer: {
    paddingHorizontal: 20,
    marginBottom: 15,
  },
  wrapper: {
    borderWidth: 1,
    borderColor: Colors.grey,
    borderRadius: 3,
    borderBottomWidth: 1,
  },
};