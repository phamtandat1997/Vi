const TYPE = {
  ROOT: 'ROOT',
  MENU: 'MENU',
  CHILD: 'CHILD',
};
const BackHandlerProvider = ({ children }) => children || null;

export const TYPE_NAVIGATION = TYPE;
export default BackHandlerProvider;
